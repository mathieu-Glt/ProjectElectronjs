// const { versions } = require("process")

// const information = document.getElementById('info')
// console.log('====================================');
// console.log(information);
// console.log('====================================');
// information.innerText = `This app is using Chrome (v${versions.chrome()}), Node.js (v${versions.node()}), and Electron (v${versions.electron()} and context frame (v${versions.http()}))`

// const func = async () => {
//     const response = await window.versions.ping()
//     console.log(response) // prints out 'pong'
// }
  
// func()